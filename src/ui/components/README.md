UI components that are fully decoupled from live call-control objects should live here.

Current dashboard components remain in their existing folders while the call engine is migrated behind the WebPhone/Calls providers.
