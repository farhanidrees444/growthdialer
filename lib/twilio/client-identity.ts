/**
 * Twilio Client identity used by the browser Device and inbound webhook routing.
 * Must match the <Client> name in /api/twilio/webhook TwiML.
 */
export const TWILIO_CLIENT_IDENTITY = 'agent_farhan';
